[pytest]
testpaths = tests
# Импорты webapp рассчитаны на запуск из корня репо (bot_bridge хакает sys.path,
# материалы/шаблоны ищутся относительно cwd). Тесты — так же.
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -q
